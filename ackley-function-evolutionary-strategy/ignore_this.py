### coisas que podemos querer usar, mas por enqt nao precisa, deixa aqui so p n deletar

  # population = [ Individual() ] * population_size # definir population_size
  # total_iterations = 0
  # successful_mutations = 0
  # sigma = 1
  
  #for iteration in range(number_of_iterations): #definir SOMETHING
    # if (total_iterations > 0):
    #   sigma = get_sigma(sigma, successful_mutations / total_iterations) #PRIMEIRO SIGMA <<<<<<
    
    # new_population = []

    # for individual in population:
    #   z = generate_array_from_normal_distribution(ACKLEY_N, 0, sigma)
      
    #   candidate = Individual(individual.fenotype + z) # + tá definida pra np arrays # DEPOIS O X <<<<<
      
    #   if(individual.fitness() > candidate.fitness()): # minimize
    #     new_population.append(candidate)
    #   else:
    #     new_population.append(individual)

    # total_iterations += 1